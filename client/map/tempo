function Window() {
  let members = {
  }
  
  let methods = {
  }

  let tick = function() {
  }

  let tickInterval = 100

  let obj = {
    ...members,
    ...methods,
    tick: setInterval(tick(), tickInterval);
  };
}
